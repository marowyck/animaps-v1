# ANIMAPS web — environment variables
# Copy to .env.local for local overrides. None are required in Phase 1
# (waitlist uses placeholder persistence; GA4 is not wired yet).

# NEXT_PUBLIC_GA_MEASUREMENT_ID=
# DATABASE_URL=
