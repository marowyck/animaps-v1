# apps/api — never commit real secrets.
# Copy to apps/api/.env (gitignored) or use root .env with dotenv path.

PORT=3001
DATABASE_URL=postgresql://animaps:animaps@localhost:5432/animaps

# CORS origin for apps/web (credentials / cookies later)
WEB_ORIGIN=http://localhost:3000
