# Future apps/api — never commit real secrets.
# Copy to .env (gitignored) or apps/api/.env.

DATABASE_URL=postgresql://animaps:animaps@localhost:5432/animaps
PORT=3001
WEB_ORIGIN=http://localhost:3000
